.DS_Store
*.log
node_modules/

# P0 spike scratch — holds UNSCRUBBED repo names and session titles.
# Scrubbed subset is frozen in test/fixtures/collector/ (see P0-5).
spike/
dist/
coverage/
.vitest/

# Auto-generated tool output. CODE_INTEL.md was swept into a commit by a careless `git add -A`
# during the daycap rename; it is a 5-line stub that says "project not yet indexed" and names the
# pre-rename repo. Ignored so the next `-A` cannot do it again.
CODE_INTEL.md
